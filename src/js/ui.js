import { toggleFullscreen } from './fullscreen'
import { checkSwitch } from './check-switch'

document.addEventListener("DOMContentLoaded", function() {
    console.log("Loaded OPUI scripts", toggleFullscreen, checkSwitch);
});
